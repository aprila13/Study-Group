function Tutorial() {

  // variable declaration
  // let element = <h1>hello</h1>;
  // console.log(element.props.children)

  // Declaring a variable then using inside of JSX by wrapping with curly braces
  // let name = 'April Craig';
  // let element = <h1>Hello {name}</h1>

  return (
    <div>
      {/* rending the element to the dom */}
      {/* {element} */}

      {/* {element} */}
    </div>
  );
}

export default Tutorial;
